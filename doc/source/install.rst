Installation
============

RankSEG can be installed easily via pip:

.. code-block:: bash

    pip install rankseg

Or directly from the source for the latest development version:

.. code-block:: bash

    pip install git+https://github.com/username/rankseg.git

Requirements
------------

* PyTorch >= 2.0.0
* torchvision
* numpy
